// Estilos da tela de Alugados

import { StyleSheet } from "react-native";

export const rentedStyles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
    
    title: {
      fontSize: 22,
      fontWeight: 'bold',
    }
});