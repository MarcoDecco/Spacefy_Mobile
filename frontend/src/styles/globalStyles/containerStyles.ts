import { StyleSheet } from "react-native";

export const containers = StyleSheet.create({
    container1: {
        flex: 1,
    },
    
    container2: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    }
})