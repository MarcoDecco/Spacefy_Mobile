import { StyleSheet } from 'react-native';

export const splashStyles = StyleSheet.create({
  gradient: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },

  logo: {
    width: 250,
    height: 40,
    resizeMode: 'contain',
    marginBottom: 60,
  },
}); 